function somarNumeroComDois(numero) {
    return numero + b
}

try {
    converteParaNumero('AsasaAaSASAa');
} catch(err) {
    console.log('Não foi possível somar o numero com 2 ...' + err);
} finally {
    console.log('Processado de soma terminado.')
}